# genetic-algorithms
Framework for genetic algorithms



## TODO
* [] Documentation
* [] "ObjectiveFunction" enum for controlling which objective function to use (i.e. MSEFunction, LinearFunction, CustomFunction, ect...)
* [x] Make "seed" a Option<u64> instead of fixing it to a u64 in order to have the ability to either use the seed or not


